## 1. Fix source panel transparency

- [x] 1.1 Add `bg-background` class to the `<aside>` element in `src/routes/anime/$id/episode/$ep.tsx` so the inline source panel has an opaque background on desktop

## 2. Verification

- [x] 2.1 Verify on desktop that the source panel is opaque and the player area remains transparent for mpv rendering
